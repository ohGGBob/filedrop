plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
}

android {
    namespace 'com.ohggbob.filedrop'
    compileSdk 35

    // 版本由 CI 从 core/server.go 的 Version 常量提取后注入（-PversionName=），
    // versionCode 用 git 提交数，保证单调递增。本地无参数时用兜底值。
    def vName = project.findProperty('versionName') ?: '0.7.0'
    def vCode = (project.findProperty('versionCode') ?: '11').toInteger()

    defaultConfig {
        applicationId "com.ohggbob.filedrop"
        minSdk 26
        targetSdk 35
        versionCode vCode
        versionName vName
    }

    // 自签 keystore 随仓库提交：保证任何人构建出的 APK 签名一致，可覆盖升级。
    // 这是局域网互传工具，keystore 不承担安全职责，固定密码仅为可复现构建。
    signingConfigs {
        release {
            storeFile rootProject.file("release.keystore")
            storePassword "filedrop2026"
            keyAlias "filedrop"
            keyPassword "filedrop2026"
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            signingConfig signingConfigs.release
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = '17'
    }
}

dependencies {
    implementation files('libs/filedrop.aar')
    implementation 'androidx.core:core-ktx:1.13.1'
    implementation 'androidx.appcompat:appcompat:1.7.0'
    implementation 'androidx.webkit:webkit:1.11.0'
}
