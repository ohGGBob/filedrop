# 隐私说明

FileDrop 是纯局域网点对点文件传输工具。

- 不收集、不上传任何个人信息到云端或第三方服务器
- 所有文件与便签仅在你的设备与授权的局域网设备之间传输
- 配对令牌仅存于本机 `filedrop-config.json`，不通过广播发送
- 便签读写均需令牌，临时授权绑定来源 IP 且 30 分钟过期
- 局域网发现报文仅含主机名/IP/端口/版本，不含令牌
- 如需彻底清理，删除 `filedrop-config.json` 与 `filedrop-notes.json` 即可

如有问题请提交 Issue。
