# 构建产物：由 go build 生成，不入库
/filedrop.exe
/filedrop-tray.exe
*.exe
*.exe~

# 运行时数据
received/
filedrop-config.json
filedrop-notes.json
filedrop-notes.json.tmp

# 自测生成的大文件
*.bin
*.bin.sha256
big*.bin

# Go / 工具缓存
.gocache/
